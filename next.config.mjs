/** @type {import('next').NextConfig} */
// const nextConfig = {};
//

/** @type {import('next').NextConfig} */
const nextConfig = {
    // experimental: {
    //     serverComponentsExternalPackages: ['pg-native' ,'bcrypt', 'bcryptjs', 'pg'],
    // },
    // webpack: (config) => {
    //     config.externals = [...config.externals, 'bcrypt', 'pg-native'];
    //     return config;
    // },
};

// module.exports = nextConfig;
export default nextConfig;
